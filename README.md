# VibeCodingDemo

一个基于 **C# + Blazor Server + Microsoft Semantic Kernel + Ollama** 的 AI 自动编程演示工具。

## 运行

```bash
cd VibeCodingDemo
dotnet restore
dotnet run
```

默认 Ollama 配置位于 `VibeCodingDemo/appsettings.json`。
